import { IQuerist } from './Queries/IQuerist';
export interface QueryOption<T, K = keyof T> {
    name: K;
    singleRow?: boolean;
    pickField?: string;
    template: string;
    tag?: string;
    autoType?: boolean;
    type?: 'number' | 'string' | 'date' | 'bigint' | 'boolean';
}
export declare type Caller<T extends QueryFunType> = {
    [P in keyof T['Input']]: (param: T['Input'][P]) => Promise<T['Result'][P]>;
};
export declare type QueryFunType = {
    Input: any;
    Result: any;
};
export declare class QueryFun<T extends QueryFunType> {
    private static queries;
    static register: (querist: IQuerist<any>) => Promise<void>;
    private static instance;
    static create: (path: string, format?: 'json' | 'yaml') => QueryFun<any>;
    static query: (sql: string, tag?: string) => Promise<any>;
    private options;
    private defOption;
    private strFun;
    constructor(options: QueryOption<T['Input'], keyof T['Input']>[]);
    private getParams;
    private static handler;
    private static createHandler;
    private initQuerier;
    querier: Caller<T>;
    demo: () => Promise<any>;
    strDemo: () => {
        [x: string]: any;
    };
    close: () => void;
    exportInterface: () => Promise<{
        Result: any;
        Input: {
            [x: string]: any;
        };
    }>;
}
