import { LogConfig } from 'sls-client';
import { IQuerist, IQueristType } from './IQuerist';
export declare type SlsOption = LogConfig & {
    tag: string;
    type: IQueristType;
};
export default class SlsQuerist extends IQuerist<SlsOption> {
    init: () => Promise<void>;
    private client;
    constructor(option: SlsOption);
    type: IQueristType;
    private getQueryOption;
    close: () => void;
    query: <R = any>(sql: string) => Promise<R>;
    tag: string;
}
